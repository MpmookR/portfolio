# SPA fallback for Netlify.
# Any URL that doesn't match a real file gets served index.html with a 200,
# so React Router can handle the route on the client side.
# Without this, refreshing or directly visiting any route returns a 404.
/* /index.html 200
